import {Button} from "react-bootstrap";

export default function Login(){
    return (
        <div>
            <p>hello</p>
            <Button variant={"secondary"}>boo</Button>
        </div>
    );
}